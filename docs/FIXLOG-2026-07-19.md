# Diario fix — 19 luglio 2026

> **Questo è il posto ordinato** per tutto ciò che è stato sistemato/verificato
> in questa giornata. Per l’indice generale dei docs: [INDICE.md](INDICE.md).

---

## Come è organizzato

1. [Path CFD](#1-path-cfd-botpy) — conto CFD (`IG_*`), file `bot.py`
2. [Path opzioni](#2-path-opzioni-run_spread--demone) — conto opzioni (`IG_LIVE_*` / DEMO opzioni)
3. [Tracking / pulizia](#3-tracking-e-pulizia)
4. [Test fatti oggi](#4-test-fatti-oggi)
5. [Cosa non toccare ancora](#5-cosa-non-toccare-ancora)
6. [Mappa issue](#mappa-issue)

**I due conti IG sono separati.** Non usare il path CFD per testare le opzioni e viceversa.

---

## 1. Path CFD (`bot.py`)

> Contatto: equity DEMO/LIVE CFD. Strategie: dip-buy (Antonio: in prospettiva
> non userà questo edge in prod; il codice resta sistemato).

| # | Titolo breve | Stato | Cosa fa ora | Dove |
|---|--------------|-------|-------------|------|
| [#3](https://github.com/IGEdge/igedge/issues/3) | Scale-in ADD non bloccato | chiusa | `allow_stack=True` su ADD | `src/core/order_manager.py`, `bot.py` |
| [#4](https://github.com/IGEdge/igedge/issues/4) | Intraday + max hold cablati | chiusa | EOD flat, time-exit, rientro | `bot.py`, `dip_buy.py`, `position_store.py` |
| [#5](https://github.com/IGEdge/igedge/issues/5) | Plan-only di default | chiusa | Ordini solo con `--arm` (+ understand su LIVE) | `bot.py` |
| [#8](https://github.com/IGEdge/igedge/issues/8) | Sizing unità uguali | chiusa | `size_for(..., units=1+scale_in)` | `src/core/risk_manager.py` |
| [#10](https://github.com/IGEdge/igedge/issues/10) | Sessione + throttle CFD | chiusa | `PersistentIGSession` + `ThrottledClient` | `bot.py` |
| [#12](https://github.com/IGEdge/igedge/issues/12) | Orari RTH | chiusa | No ENTER/ADD fuori 09:30–16:00 NY | `src/core/market_hours.py`, `bot.py` |
| [#9](https://github.com/IGEdge/igedge/issues/9) | Health-check API | chiusa oggi | Ping + allarme **LOG only** — **mai flat auto** | `src/core/health_check.py`, `bot.py` |

Env CFD rilevanti (`.env.example`):

- `BOT_ARM`, `BOT_I_UNDERSTAND_LIVE_RISK`
- `DIP_INTRADAY`, `DIP_MAX_HOLD_DAYS`, `DIP_FLAT_BEFORE_CLOSE_MIN`
- `HEALTH_CHECK_INTERVAL_SEC`, `MAX_API_DOWN_SEC` (solo allarme log)
- `CFD_THROTTLE_SEC`

Test mock CFD:

```bash
python scripts/test_order_manager_add.py   # #3
python scripts/test_health_check.py        # #9
```

---

## 2. Path opzioni (`run_spread` / demone)

> Contatto: conto opzioni. DEMO = senza `--live` (`IG_*`). LIVE = `--live` (`IG_LIVE_*`).

| # | Titolo breve | Stato | Cosa fa ora | Dove |
|---|--------------|-------|-------------|------|
| [#7](https://github.com/IGEdge/igedge/issues/7) | Margine 50% pre-apertura | chiusa | Skip se impegno > 50% conto; strict se armato | `src/options/spread_orchestrator.py` |
| [#15](https://github.com/IGEdge/igedge/issues/15) | Arm per-strategia (modulare) | **aperta** (meccanismo ok; attivazione prod no) | Allowlist config; demone non hardcoda la put | `src/options/arming.py`, `run_spread.py`, `sampler_daemon.py` |
| [#16](https://github.com/IGEdge/igedge/issues/16) | Rename Condor→Spread | **aperta** (API Python fatta; SQL/legacy restano) | Nomi generici + alias; vedi `docs/RENAME-CONDOR.md` | `spread.py`, store/executor/monitor, `monitor_spreads.py` |

### Guida dedicata arm

→ **[ARM-PER-STRATEGIA.md](ARM-PER-STRATEGIA.md)**

Riassunto:

| Conto | Per armare |
|-------|------------|
| DEMO | `--arm` |
| LIVE | `--arm` + `--live` + `--i-understand-live-risk` |

Allowlist: `OPTIONS_ARMED_STRATEGIES` o `--arm-strategies`.  
Demone default: `OPTIONS_DAEMON_ARM=false` → **come prima, nessun ordine automatico**.

Test mock + prova DEMO (19 lug, domenica):

```bash
python scripts/test_arming_policy.py
python scripts/run_spread.py --strat putspread --arm --vix 22 --vix10max 30
# → ARMATO, piano ok, abort EDITS_ONLY (mercato chiuso) — errore chiaro
```

---

## 3. Tracking e pulizia

| # | Cosa | Stato |
|---|------|-------|
| [#13](https://github.com/IGEdge/igedge/issues/13) | Telegram | chiusa — **fuori scope** (enhancement non previsto) |
| [#1](https://github.com/IGEdge/igedge/issues/1) | Test issuebeam | chiusa |

---

## 4. Test fatti oggi

| Test | Esito | Note |
|------|-------|------|
| `test_arming_policy.py` | OK | Solo policy allowlist |
| `test_health_check.py` | OK | Solo telemetria |
| `test_order_manager_add.py` | OK | ADD / already_open |
| Apertura put DEMO armata | OK come prova | `EDITS_ONLY`, nessun fill; **non** CFD |

Non eseguito oggi: sync Pi, job demone in prod, apertura LIVE.

---

## 5. Cosa non toccare ancora

| Topic | Issue | Perché |
|-------|-------|--------|
| Sync codice sul Pi | (operativo) | Campagna gate in corso — sync alla ripresa |
| Uscita C8 call | [#6](https://github.com/IGEdge/igedge/issues/6) | Rimandata (maturazione) |
| Scheduler report/manage | [#11](https://github.com/IGEdge/igedge/issues/11) | Parziale: arm modulare in #15; manca report posizioni |
| Ensemble CFD live | [#14](https://github.com/IGEdge/igedge/issues/14) | Dopo / se CFD usato |
| Multi-broker | [#2](https://github.com/IGEdge/igedge/issues/2) | Dopo validazione IG |
| Attivazione put in prod | [#15](https://github.com/IGEdge/igedge/issues/15) | Flag ancora off; checklist nell’issue |

---

## Mappa issue

### Chiuse (fix presenti nel codice sul PC)

`#3` `#4` `#5` `#7` `#8` `#9` `#10` `#12` `#13` (scope rifiutato)

### Aperte rilevanti

`#15` (arm prod) · `#6` (C8) · `#11` (scheduler residuo) · `#14` · `#2`

---

## File nuovi / toccati oggi (riferimento rapido)

**Nuovi**

- `src/core/health_check.py`
- `src/options/arming.py`
- `scripts/test_health_check.py`
- `scripts/test_order_manager_add.py`
- `scripts/test_arming_policy.py`
- `docs/ARM-PER-STRATEGIA.md`
- `docs/INDICE.md`
- `docs/FIXLOG-2026-07-19.md` (questo file)

**Aggiornati (tra gli altri)**

- `bot.py` — arm CFD, RTH, intraday, health, sessione
- `scripts/run_spread.py` — arm per-strategia, gate DEMO/LIVE, `--vix10max`
- `scripts/sampler_daemon.py` — gate demone da env
- `.env.example`, `docs/STATO-PROGETTO.md`, `docs/ARCHITETTURA-BOT.md`
- `deploy/sampler-opzioni/README.md`, `docker-compose.yml` (commenti)
